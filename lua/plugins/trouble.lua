return {
  "folke/trouble.nvim",
  opts = {
    focus = true,
    keys = { ["<cr>"] = "jump_close", o = "jump" },
  }, -- for default options, refer to the configuration section for custom setup.
  cmd = "Trouble",
}
